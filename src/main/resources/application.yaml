spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  datasource:
    url: jdbc:postgresql://localhost:5433/mydatabase
    username: postgres
    password: 11111
  flyway:
    baseline-on-migrate: true

server:
  port: 8080